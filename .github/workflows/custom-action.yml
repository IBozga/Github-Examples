on:
  push:
    branches:
      - main

jobs:
  my_job:
    name: A job to say hello
    runs-on: ubuntu-latest

    steps:
      - name: Hello world action step
        id: hello
        uses: IBozga/mars@0.0.2
        with:
          name: Marsian

      # Use the output from the `hello` step
      - name: Get the output
        run: echo "The time was ${{ steps.hello.outputs.greeting }}"
